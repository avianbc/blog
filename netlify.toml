[build]
command = "cd dashboard && pnpm install --frozen-lockfile && pnpm run build && cd .. && hugo"
publish = "public"

[build.environment]
HUGO_VERSION = "0.153.1"
NODE_VERSION = "22"

[[redirects]]
from = "/dashboard/*"
to = "/dashboard/index.html"
status = 200
